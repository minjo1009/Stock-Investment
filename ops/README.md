# Ops Governance

This directory contains repo-level Codex governance files for task scope, document lifecycle, context bundles, validators, and the static mission-control dashboard.

Use `ops/task_registry.yaml` as the task source of truth, `ops/doc_registry.yaml` as the document lifecycle source of truth, and `ops/task_profiles.yaml` for profile-specific rules.
