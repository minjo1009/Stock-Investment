# Skill MD Subagent Canonicalization Map

## Purpose

This document is the human-readable summary of Task748.

It answers:

```text
Which skills, markdown operating documents, and subagent routes govern future work?
Which documents must preserve source-code and test-authority boundaries?
What must GPT/Chrome and subagents never overclaim?
```

The full machine-readable table is:

`docs/reports/task_748_skills_md_subagent_canonicalization/task748_skill_md_subagent_inventory.csv`

## Current Counts

| Surface | Count |
| --- | ---: |
| operating_system | 16 |
| architecture | 11 |
| skill | 8 |
| ownership | 7 |
| contract | 6 |
| root_agent_rules | 1 |

| Authority Level | Count |
| --- | ---: |
| SUPPORTING_DOC | 28 |
| OPERATING_RULE | 12 |
| CANONICAL_REFERENCE | 9 |

## Result

Task748 ends with:

```text
required_next_action = 0
```

This means no immediate structural follow-up was detected in the skills/MD/subagent inventory.

It does not mean:

- research validity
- strategy acceptance
- deployment readiness
- broker truth completion
- real-capital permission

## Canonical Operating Inputs

Future Codex sessions should use:

1. `docs/operating_system/project_operating_state.md`
2. `docs/architecture/project_status_authority_matrix.md`
3. `docs/architecture/src_canonicalization_map.md`
4. `docs/architecture/test_validation_canonicalization_map.md`
5. `docs/ownership/subagent_roster_and_routing.md`
6. `docs/ownership/subagent_packet_standard.md`
7. `skills/skill.md`
8. `skills/gpt-chrome-review-subagent/SKILL.md`

## GPT/Chrome Boundary

GPT/Chrome may review:

- overclaims
- missing evidence
- unclear architecture
- weak decomposition
- validation wording

GPT/Chrome may not decide:

- strategy acceptance
- deployment readiness
- broker truth
- raw source correctness
- PnL validity
- buy/sell/sizing

## Subagent Boundary

Every worker packet must include:

- owner team
- reviewer team
- read scope
- write scope
- validation command
- validation authority
- what PASS does not mean

Explorers are read-only.

Workers must have disjoint write scopes.

## Test Authority Boundary

Every skill or subagent that mentions tests must use:

- `docs/architecture/test_validation_canonicalization_map.md`

Allowed fast local gate candidates:

- `PACKAGE_HEALTH`
- `GOVERNANCE_HEALTH`

Not fast local quality gates:

- `EVIDENCE_ONLY`
- `RESEARCH_ONLY`
- `SUPPORT_ONLY`
- `EXECUTION_HEALTH`
- `ACCEPTANCE_EVIDENCE_REVIEW`
- `DATA_HEALTH`
- `REPORTING_HEALTH`

## Source-Code Boundary

Every skill or subagent that touches code must use:

- `docs/architecture/src_canonicalization_map.md`

`canonical_package_candidate` is not production-ready.

`active_task_code_review` is not automatically canonical.

`historical_task_code_review` is not a current engine.
