# A001 Clarification Questions

These questions block destructive actions only.

1. Should old frontend/PWA/Expo Go evidence be archived or deleted after a summary exists?
2. Are old GPT loop prompt/response snapshots valuable as audit evidence, or can summarized duplicates become delete candidates?
3. Which large artifacts must be preserved for future backtest reproducibility even when superseded?
4. Should the frontend app be split into a separate repository later, or remain in this monorepo?
5. Should `docs/reports/` remain in place but be excluded from default read scope, or should approved historical reports be physically moved under `docs/archive/`?
6. Should `docs/INDEX.md` remain a broad navigation index while `docs/active/PROJECT_TABLE_OF_CONTENTS.md` becomes the default Codex table of contents?
7. Which current UI direction is final: the current `frontend/trader-terminal` and task report chain, or a new condensed active frontend SSOT?
8. Should stale Graphify output be regenerated in place, archived first, or deleted after a new graph is produced?
9. Should duplicate generated frontend catalogs under `frontend_data/catalog/` and `frontend/trader-terminal/public/catalog/` be merged, or must both remain for separate consumers?

