# Task3839 Loop 8 BRAIN v1 Hierarchy

## Decision Summary

Loop 8 promotes BRAIN from v0 to v1 hierarchy by reordering existing sections and cleaning copy only.

No candidate score, rank, confidence, signal, buy/entry language, filter behavior, new route, new component, fixture change, read-model change, DB/runtime connection, or broker path was added.

## Change

- BRAIN header badge now reads `BRAIN v1`.
- `Brain Decision Snapshot` is now `Brain Overview`.
- `Candidate Ideas` is now `Review Queue`.
- `Risk Gate` is now `Blocked / Missing Evidence`.
- The full Scaffold Boundary remains visible after the review/evidence sections.

## Safety Boundary

BRAIN remains scaffold-only, fixture-backed, `NOT_AUTHORITY`, and read-only.

Strategy remains `NOT_ACCEPTED`, deployment remains `DIAGNOSTIC_ONLY_NOT_DEPLOYMENT_READY`, and real capital remains `FORBIDDEN`.
