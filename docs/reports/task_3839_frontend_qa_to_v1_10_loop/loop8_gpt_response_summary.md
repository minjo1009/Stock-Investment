# Loop 8 GPT Response Summary

GPT verdict: `PASS`, hierarchy plus copy only.

Required scope:

- edit `app/(tabs)/brain.tsx` only
- move Brain Decision Snapshot, Candidate Ideas, and Risk Gate above the full Scaffold Boundary
- rename copy only:
  - Brain Decision Snapshot -> Brain Overview
  - Candidate Ideas -> Review Queue
  - Risk Gate -> Blocked / Missing Evidence

GPT prohibited score, rank, confidence, signal, buy, entry, filter behavior, new ordering logic, new components, new routes, fixture changes, and read-model changes.
