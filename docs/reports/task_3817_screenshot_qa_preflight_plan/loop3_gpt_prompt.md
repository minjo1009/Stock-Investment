# LOOP-0003 GPT Prompt

Asked Chrome GPT Agent Mode with GitHub context to review Loop 3 only: screenshot QA preflight planning, not product screen implementation. The prompt required target surfaces, device/browser matrix, state matrix, artifact naming, storage locations, acceptance criteria, forbidden evidence claims, validation checklist, and next loop recommendation while preserving `NOT_ACCEPTED`, `DIAGNOSTIC_ONLY_NOT_DEPLOYMENT_READY`, and `FORBIDDEN`.
